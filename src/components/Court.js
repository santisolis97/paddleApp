import React from "react";
import { useParams } from "react-router";

function Court() {
  //   let { id } = useParams();
  return (
    <div>
      <h1>La cortada</h1>
    </div>
  );
}

export default Court;
